[project]
name = "ha-ethereum-balance"
# Kept in step with custom_components/ethereum_balance/manifest.json. The
# release-gate job in CI fails a tag push when the two disagree.
version = "0.3.4"
description = "Ethereum balance tracker for Home Assistant using the Etherscan API"
# homeassistant 2026.3.0 and later declare requires-python >=3.14.2, so claiming
# >=3.13 alongside the homeassistant floor below makes the dependency set
# unresolvable and silently resolves CI to a year-old homeassistant instead.
requires-python = ">=3.14.2"
license = "MIT"

dependencies = [
    "homeassistant>=2026.4.0",
]

[project.optional-dependencies]
dev = [
    "homeassistant-stubs",
    "mypy",
    "pre-commit",
    "pylint",
    "pytest",
    "pytest-asyncio",
    "pytest-homeassistant-custom-component",
    "ruff",
    "codespell",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
# This repo is a Home Assistant custom component, not an installable package: the
# code lives in custom_components/ethereum_balance and HACS copies that directory
# in. Without this, `uv sync` tries to build the project as a wheel and hatchling
# fails with "Unable to determine which files to ship", because no directory
# matches the project name ha_ethereum_balance. The sibling integration repos all
# carry the same line.
package = false

[tool.ruff]
# Deliberately one release behind requires-python above. At py314 the formatter
# rewrites `except (TimeoutError, aiohttp.ClientError):` in config_flow.py into the
# PEP 758 unparenthesized form, which is a SyntaxError on anything older than 3.14.
# A HACS user on a slightly older Home Assistant would get an import crash rather
# than a lint warning, so the source stays in the portable form.
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = [
    "E",
    "F",
    "W",
    "I",
    "UP",
    "B",
    "SIM",
    "T20",
    "RUF",
]

[tool.mypy]
python_version = "3.14"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
